// Safe browser runtime configuration. Do not put service-account keys here.
export const runtimeConfig = Object.freeze({
  eventEndpoint: '/api/events',
  backendHealthEndpoint: '/api/backend-health',
});
