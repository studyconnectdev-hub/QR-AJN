# Business dashboard

This is a static, dependency-free dashboard prepared for Firebase Hosting. Hosting works on Spark; live management calls require the optional Blaze backend.

- No credentials are stored in localStorage or cookies.
- Set `ADMIN_API_KEY` on the backend and enter it only for the current browser tab.
- The privacy policy is published as `privacy.html`.
